#!/usr/bin/env python3
"""
═══════════════════════════════════════════════════════════════════════════════
🖥️ ЗАПУСК TUI АДМИН-ПАНЕЛИ
═══════════════════════════════════════════════════════════════════════════════
Точка входа для терминальной админ-панели.

Запуск: python run_admin.py
═══════════════════════════════════════════════════════════════════════════════
"""

import sys
import asyncio
from pathlib import Path

# Добавляем корневую папку в путь
sys.path.insert(0, str(Path(__file__).parent))


def main():
    """Главная функция запуска TUI."""
    try:
        # Импортируем TUI приложение
        from tui.app import AdminApp
        
        # Создаём и запускаем приложение
        app = AdminApp()
        app.run()
        
    except ImportError as e:
        print("=" * 60)
        print("❌ ОШИБКА ИМПОРТА")
        print("=" * 60)
        print(f"Модуль не найден: {e}")
        print()
        print("Убедитесь, что установлены зависимости:")
        print("  pip install textual rich")
        print()
        print("Или запустите install.bat")
        print("=" * 60)
        sys.exit(1)
        
    except Exception as e:
        print("=" * 60)
        print("❌ ОШИБКА ЗАПУСКА TUI")
        print("=" * 60)
        print(f"Ошибка: {e}")
        print("=" * 60)
        sys.exit(1)


if __name__ == "__main__":
    main()
